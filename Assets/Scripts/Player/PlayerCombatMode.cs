namespace ReturnVector.Player
{
    /// <summary>
    /// Tracks whether the player currently has access to the weapon.
    /// </summary>
    public enum PlayerCombatMode
    {
        Armed = 0,
        Unarmed = 1,
        Disabled = 2
    }
}

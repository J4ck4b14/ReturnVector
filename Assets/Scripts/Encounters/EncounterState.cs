namespace ReturnVector.Encounters
{
    /// <summary>
    /// Lifecycle states for an encounter.
    /// </summary>
    public enum EncounterState
    {
        Waiting = 0,
        Active = 1,
        Complete = 2
    }
}
